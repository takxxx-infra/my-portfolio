version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run buil
        - echo "start s3 sync from content/projects to s3://${SOURCE_BUCKET}/projects/"
        - aws s3 sync content/projects "s3://${SOURCE_BUCKET}/projects/" --delete --exclude "*" --include "*.md" --exclude "*template.md"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
